@model OnlineAlumniPortalMVC.Models.tblUniversityCampu

@{
    ViewBag.Title = "AddUniversityCampus";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@{
    if (Model == null)
    {
        <script>
            function createUrl(evt) {
                var Value = $("#Name").val();
                Value = Value.toLowerCase();
                Value = Value.replace(/[^A-Z0-9]/ig, "-");
                $("#Url").val(Value);
            }
        </script>
    }
}

<div class="col-xs-9">
    <h2>Add <strong>University Campus</strong></h2>
</div>
<div class="col-xs-3">
    <input type="button" value="Manage University Campuses" style="float:right;" class="btn btn-dark" data-toggle="modal" data-target="#modal-select" onclick="location.href = '/UniversityCampus/ManageUniversityCampuses'">
</div>
<form method="post" enctype="multipart/form-data" id="FormUser">

    <div class="row">
        <div class="col-lg-12 portlets">
            <div class="panel">
                <div class="panel-content">
                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label class="form-label">Title</label>
                                @Html.TextBoxFor(x => x.Title, new { @placeholder = "Enter Title", @class = "form-control", @required = "required", @maxlength = "200", @onkeyup = "return createUrl(event)" })
                            </div>
                            <div class="form-group">
                                <input type="file" id="file" name="file" />

                                @{
                                    if (Model != null)
                                    {
                                        <img src="@Model.Photo" height="300" width="400" />
                                        <input type="hidden" name="ImageName" value="@Model.Photo" />
                                    }
                                }
                            </div>
                        </div>

                    </div>
                    <div class="form-group">
                        Active

                        @Html.CheckBoxFor(x => x.isActive, new { @checked = "checked" })

                    </div>
                </div>

                <div>
                    @{
                        string Text = "Add University Campus";
                        if (Model != null)
                        {
                            Text = "Update University Campus";
                        }
                    }


                    <input type="submit" id="btnSave" class=" btn btn-dark" value="@Text" name="Save" onclick="this.form.submit(); this.disabled = true;" />
                </div>
            </div>

        </div>
    </div>
</form>




